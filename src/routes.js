const routes = {
    home: "/",
    explorer: "/explorer",
    wallet: "/wallet",
    block: "/block/:hash",
    blocks: "/blocks",
    transactions: "/transactions",
    transaction: "/transaction/:hash",
}

export default routes