const routes = {
  home: "/",
  explorer: "/explorer",
  wallet: "/wallet",
  block: "/block/:id",
  blocks: "/blocks",
  transactions: "/transactions",
  transaction: "/transaction/:id"
}

export default routes